package com.ethernet389.mai

import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.Create

object Constant {
    val navigationLabels = with(Icons.Filled) {
        listOf(
            R.string.notes to this.Create
        )
    }
}
